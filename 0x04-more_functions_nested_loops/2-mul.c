#include "main.h"

/**
 * mul - checks for lowercase character.
 * @a: a is the first argument passed to the funtion..
 * @b: b is the second argument passed to the function.
 * Return: 1 if c is uppercase
 */
int mul(int a, int b)
{
	return (a * b);
}
